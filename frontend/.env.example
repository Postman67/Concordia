# Federation API base URL — set to your deployed Federation service URL on Railway
# VITE_ variables are baked into the bundle at build time, so set this in
# Railway → Variables BEFORE triggering a deploy.
VITE_FEDERATION_BASE_URL=https://your-federation-service.up.railway.app

# Token key used in localStorage
VITE_TOKEN_KEY=concordia_token
